import { createBrowserRouter } from 'react-router-dom';

/**
 * Placeholder routes for Phase A. Real pages land in Phase D and F.
 * We intentionally keep the route table here so adding a page is one line.
 */
export const router = createBrowserRouter([
  {
    path: '/',
    element: <div className="min-h-screen p-8">School Mapping System — scaffold OK</div>,
  },
]);
